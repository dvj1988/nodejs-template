const validate = require('./validate');
const getPageInfo = require('./getPageInfo');
const getPaginationProps = require('./getPaginationProps');

module.exports = {
  validate,
  getPageInfo,
  getPaginationProps
};
